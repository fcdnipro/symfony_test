{% extends 'base.html.twig' %}

{% block title %}Edit Company{% endblock %}

{% block body %}
    <h1>Edit Company <a class='btn btn-primary' href="{{ path('company_index') }}">Back to list</a></h1>

    {{ include('company/_form.html.twig', {'button_label': 'Update','button_class': 'btn btn-primary'}) }}

    {{ include('company/_delete_form.html.twig') }}
{% endblock %}
