{% extends 'base.html.twig' %}

{% block title %}New Employees{% endblock %}

{% block body %}
    <h1>Create new Employees <a class="btn btn-primary" href="{{ path('employees_index') }}">Back to list</a></h1>

    {{ include('employees/_form.html.twig') }}


{% endblock %}
