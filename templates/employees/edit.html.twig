{% extends 'base.html.twig' %}

{% block title %}Edit Employees{% endblock %}

{% block body %}
    <h1>Edit Employees <a class='btn btn-primary' href="{{ path('employees_index') }}">Back to list</a></h1>

    {{ include('employees/_form.html.twig', {'button_label': 'Update','button_class': 'btn btn-primary'}) }}

    {{ include('employees/_delete_form.html.twig') }}
{% endblock %}
